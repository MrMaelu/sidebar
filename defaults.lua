local defaults = T{}

defaults.Tarus = 0
defaults.Taru_speed = {5, 15}
defaults.Taru_size = 100

defaults.Theme = {}
defaults.Theme.Name = 'Sidebar_5_1'
defaults.Theme.Size = {500, 100}
defaults.Theme.Scale = 1.0

defaults.Texts = {}
defaults.Texts.Font = 'Rockwell'
defaults.Texts.Size = 20

defaults.Texts.Color = {}
defaults.Texts.Color.Red = 255
defaults.Texts.Color.Green = 255
defaults.Texts.Color.Blue = 255

defaults.Texts.Stroke = {}
defaults.Texts.Stroke.Width = 3
defaults.Texts.Stroke.Red = 50
defaults.Texts.Stroke.Green = 50
defaults.Texts.Stroke.Blue = 50

defaults.Texts.Offset = {}

-- Att / def
defaults.Texts.Offset.ATT = {25, 25} --Attack
defaults.Texts.Offset.DEF = {25, 25} --Defense

-- Attributes
defaults.Texts.Offset.STR = {25, 25}
defaults.Texts.Offset.DEX = {25, 25}
defaults.Texts.Offset.VIT = {25, 25}
defaults.Texts.Offset.AGI = {25, 25}
defaults.Texts.Offset.INT = {25, 25}
defaults.Texts.Offset.MND = {25, 25}
defaults.Texts.Offset.CHR = {25, 25}

-- Resistances
defaults.Texts.Offset.RES_FIR = {25, 25} --Fire
defaults.Texts.Offset.RES_WIN = {25, 25} --Wind
defaults.Texts.Offset.RES_THU = {25, 25} --Lightning (thunder)
defaults.Texts.Offset.RES_ICE = {25, 25} --Ice
defaults.Texts.Offset.RES_EAR = {25, 25} --Earth
defaults.Texts.Offset.RES_WAT = {25, 25} --Water
defaults.Texts.Offset.RES_LIG = {25, 25} --Light
defaults.Texts.Offset.RES_DAR = {25, 25} --Dark

-- Others
defaults.Texts.Offset.MJ  = {25, 25} --main job
defaults.Texts.Offset.SJ  = {25, 25} --sub job
defaults.Texts.Offset.MJL = {25, 25} --main job level
defaults.Texts.Offset.SJL = {25, 25} --sub job level

defaults.Texts.Offset.EXP = {25, 25}
defaults.Texts.Offset.TNL = {25, 25}
defaults.Texts.Offset.AREA = {25, 25}
defaults.Texts.Offset.EXPH = {25, 25}

return defaults